import { CheckIfOrderFinishedStatusLinePipe } from './check-if-order-finished-status-line.pipe';

describe('CheckIfOrderFinishedStatusLinePipe', () => {
  it('create an instance', () => {
    const pipe = new CheckIfOrderFinishedStatusLinePipe();
    expect(pipe).toBeTruthy();
  });
});
