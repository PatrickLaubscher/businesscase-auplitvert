import { TotalOrderPipe } from './total-order.pipe';

describe('TotalOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
