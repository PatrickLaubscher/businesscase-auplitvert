import { ExistingAttributionPipe } from './existing-attribution.pipe';

describe('ExistingAttributionPipe', () => {
  it('create an instance', () => {
    const pipe = new ExistingAttributionPipe();
    expect(pipe).toBeTruthy();
  });
});
