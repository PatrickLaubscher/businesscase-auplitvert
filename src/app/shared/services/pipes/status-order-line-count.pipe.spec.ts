import { StatusOrderLineCountPipe } from './status-order-line-count.pipe';

describe('StatusOrderLineCountPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusOrderLineCountPipe();
    expect(pipe).toBeTruthy();
  });
});
